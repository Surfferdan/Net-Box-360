{
  "name": "netbox-client-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client SDK connecting the existing Three.js Xbox 360 dashboard to the NetBox API Gateway (REST + WebSocket).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --test tests/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
